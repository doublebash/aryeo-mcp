# Dependencies
node_modules/

# Build artefacts
dist/

# Wrangler local state (auth, build cache)
.wrangler/

# Local-only environment vars — NEVER commit. Real secrets live in Cloudflare.
.dev.vars
.dev.vars.local

# OS / editor noise
.DS_Store
*.log

# Output / scratch dirs
/tmp/
*.tmp
